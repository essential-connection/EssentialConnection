@model X.PagedList.IPagedList<EssentialConnection.Models.Curriculo>
@using X.PagedList.Mvc.Core

@{
    ViewData["Title"] = "Interesse em curriculo";
}
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/EssentialConnection.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/Home.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/app.min.css" asp-append-version="true" />
</head>
<div class="h-full w-full flex justify-center md:justify-between items-center">
    <div class="hidden h-full lg:flex lg:block w-1/5  bg-indigo-900 px-10 justify-center">
    </div>
    <div class="w-full px-5 flex flex-col items-center">
        <div class="mb-12">
            <p class="text-3xl text-gray-700 uppercase">Marque interesse em algum estudante</p>
        </div>
        <div class="bg-gradient-to-r from-gray-200 via-slate-100 to-gray-200 py-6 text-black h-auto min-h-full rounded shadow-lg shadow-gray-800 border-2 border-indigo-400 lg:border-l-0 lg:border-t lg:border-indigo-400 rounded-b lg:rounded-b-none lg:rounded-r" style="text-align:center">
            @foreach (var item in Model)
            {
                <h1 class="text-3xl text-gray-700 uppercase">
                </h1>
                <p class="text-3xl text-gray-700 uppercase">
                    @Html.DisplayFor(modelItem => item.Aluno.NomeCompleto)
                </p>
                <hr>
                <div style="width:500px;word-wrap:break-word">
                    <p class="py-4 pl-4" style="font-size:17px">
                        @Html.DisplayFor(modelItem => item.DescricaoPessoal)
                    </p>
                </div>
                
                <div class="grid grid-lines grid-cols-2 auto-rows-auto gap-3 ">
                @foreach (var item2 in item.Compentencias)
                {
                    <ul>
                        <li class="mb-3.5 ml-4 flex flex-wrap">
                            <span class="mr-2 my-1 rounded-full border px-4 text-sm py-2 font-medium bg-gray-300">@Html.DisplayFor(mobelItem  => item2.Descricao)</span>
                        </li>
                    </ul>
                }
                
            </div>
                <div>
                    <a asp-action="MostrarPerfilAluno" class="bg-gray-200">Ver Curriculo</a>
                </div>
                <form action="/Tinder/Index" method="post">
                    <label class="form-group pt-8 inline-flex bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded cursor-pointer">
                        <div class="form-group">
                            <input type="hidden" name="alunoId" value="@item.AlunoId">
                            <input type="hidden" name="nomeAluno" value="@item.Aluno.NomeCompleto">
                            <input type="hidden" name="empresaId" value="@item.CurriculoID">
                            @if (item.CurriculoID != null)
                            {
                                <input type="hidden" name="nomeAluno" value="@item.Aluno.NomeCompleto">
                            }
                            <input type="submit" value="Tenho Interesse" class="btn btn-primary" />
                        </div>
                        <svg class="h-10 w-10 text-green-500 hover:h-15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <polyline points="20 6 9 17 4 12" />
                        </svg>
                    </label>
                </form>
            }

            <div class="inline-flex items-center -space-x-px pt-9">
                <br />
                @Html.PagedListPager(Model,pagina=>Url.Action("Index", new{pagina}),
                new X.PagedList.Web.Common.PagedListRenderOptions{LiElementClasses = new string[]{"page-item"},
                PageClasses = new string[]{"page-link"}})
            </div>
        </div>
    </div>
    <div class="hidden h-full lg:flex lg:block w-1/5 bg-indigo-900 px-10 justify-center">
    </div>
    @*<div>
        <a asp-action="Index">Voltar</a>
        </div>*@
</div>
@*<h1>Index</h1>
<form action="/TinderEmpresa/Index" method="post">
@foreach (var item in Model) {
    <h1>Nome do aluno</h1>
    @Html.DisplayFor(modelItem=>item.Aluno.NomeCompleto)
    <h1>Descrição aluno</h1>
    <p> @Html.DisplayFor(modelItem=>item.DescricaoPessoal)</p>
    foreach (var itensInCompetencias in item.Compentencias)
    {
        <p>@itensInCompetencias.Descricao</p>
    }

    foreach (var itensCurriculo in item.ItensCurriculo)
    {
        <p>@itensCurriculo.Nome</p>
        <p>@itensCurriculo.DataInicio</p>
        <p>@itensCurriculo.DataFim</p>
        <p>@itensCurriculo.Descricao</p>
        <p>@itensCurriculo.Instituicao</p>
    }
    <input type="hidden" name="nomeCompleto" value="@item.Aluno.NomeCompleto">
    <input type="hidden" name="curriculoId" value="@item.Aluno.CurriculoId">
    <input type="submit" value="Interesse" class="btn btn-primary" />
}
</form>
<div class="row">
    @Html.PagedListPager(Model,pagina=>Url.Action("Index", new{pagina}))
</div>*@
